spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2
  flyway:
    baseline-on-migrate: true
  datasource:
    url: jdbc:h2:file:./src/main/resources/db/dev/default
    username: sa
    password:


server:
  port: 80
